<%= form_with(model: admins_health, local: true) do |form| %>
  <% if admins_health.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_health.errors.count, "error") %> prohibited this admins_health from being saved:</h2>

      <ul>
      <% admins_health.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
