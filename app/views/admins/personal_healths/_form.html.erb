<%= form_with(model: admins_personal_health, local: true) do |form| %>
  <% if admins_personal_health.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_personal_health.errors.count, "error") %> prohibited this admins_personal_health from being saved:</h2>

      <ul>
      <% admins_personal_health.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
