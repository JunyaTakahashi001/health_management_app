<%= form_with(model: users_health, local: true) do |form| %>
  <% if users_health.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_health.errors.count, "error") %> prohibited this users_health from being saved:</h2>

      <ul>
      <% users_health.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
